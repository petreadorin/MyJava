package classes.problems;

public class CheckNumber {

    //return true if x is even;

    boolean isEven(int x) {                 // int x is a paramenter
        return x % 2 == 0;                  // return value because method is not void(boolean)
    }
}
